'use strict';

describe('SkyboxApp.icLogin module', function() {

  beforeEach(module('SkyboxApp.icLogin'));

  describe('icLogin controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});